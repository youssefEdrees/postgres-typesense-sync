# postgres-typesense-sync
postgres-typesense-sync is a tool for synchronizing data from PostgreSQL tables to Typesense collections. It handles data types including vectors, JSONB, and custom transformations, with support for configurable schemas, batch processing, and operational commands.
